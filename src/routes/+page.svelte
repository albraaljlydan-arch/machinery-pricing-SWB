<!-- The root layout's guard (see +layout.svelte) handles redirecting anyone
     who lands on '/' — signed out to /login, signed in to their role's
     home. This page only ever renders for the instant before that
     redirect fires, so it's just a loading placeholder, nothing more. -->
<div style="min-height:100vh;display:flex;align-items:center;justify-content:center;color:#64748b;">
  Loading SWB System
</div>
